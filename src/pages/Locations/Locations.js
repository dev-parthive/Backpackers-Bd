import React from 'react';
import Spinner from '../../shared/Spinner/Spinner';

const Locations = () => {
    return (
        <div>
           <Spinner></Spinner>
        </div>
    );
};

export default Locations;